moduledir = pkgdatadir / meson.project_name() / 'widgets' / 'notifications'

modules_sources = [
    'item.py',
    'center.py',
    '__init__.py',
]

install_data(modules_sources, install_dir: moduledir)
