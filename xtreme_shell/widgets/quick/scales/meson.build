moduledir = pkgdatadir / meson.project_name() / 'widgets' / 'quick' / 'scales'

modules_sources = [
    'audio.py',
    'backlight.py',
    'quick.py',
    '__init__.py',
]

install_data(modules_sources, install_dir: moduledir)
