moduledir = pkgdatadir / meson.project_name() / 'widgets' / 'quick' / 'buttons'

modules_sources = [
    'audio.py',
    'bluetooth.py',
    'dnd.py',
    'network.py',
    'tray.py',
]

install_data(modules_sources, install_dir: moduledir)
